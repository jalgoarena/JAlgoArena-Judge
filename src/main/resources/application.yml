logging:
  path: ./logs

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:5000/eureka/}
---
spring:
  profiles: heroku
eureka:
  instance:
    preferIpAddress: true

logging:
  level:
    ROOT: INFO
    org.springframework.web: INFO
